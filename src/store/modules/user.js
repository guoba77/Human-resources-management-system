/**
 * 存储登录人信息
 */
export default {
  // 开启命名空间（模块化）
  namespaced: true,
  // 定义数据变量（全局共享的）（和组件data类似的：私有）=》相同点：响应式的
  state: {},
  // 修改state定义的变量（只能通过它修改）=》特点：同步
  mutations: {},
  // 获取后台数据的方法 =》特点：异步
  actions: {}
}
